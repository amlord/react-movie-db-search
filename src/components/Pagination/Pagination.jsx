import React from 'react';
import styled from 'styled-components';

const StyledPaginationContainer = styled.div`
  text-align: center;
  margin: 20px 0;
`;

const Pagination = () => (
  <StyledPaginationContainer>PAGINATION</StyledPaginationContainer>
);

export default Pagination;
